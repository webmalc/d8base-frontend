import { MainColumnDirective } from './main-column.directive';

describe('MainColumnDirective', () => {
  it('should create an instance', () => {
    const directive = new MainColumnDirective();
    expect(directive).toBeTruthy();
  });
});
